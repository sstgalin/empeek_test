import React from 'react';

import './styles.css';

const Sidebar = (props) => (
  <div className="sidebar">
    <h2 className="sidebar__title">Dairy app</h2>

    <p className="sidebar__sub-title">Comment with no sense</p>
  </div>
);

export default Sidebar;
